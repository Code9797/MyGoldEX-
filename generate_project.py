"""Generate the MyGoldEX Android prototype without overwriting existing files."""
import json
from pathlib import Path

ROOT = Path(__file__).resolve().parent / "generated" / "MyGoldEX"
FILES = json.loads(r'''{
  ".gitignore": ".gradle/\n.kotlin/\n.idea/\n**/build/\nlocal.properties\n*.jks\n*.keystore\n",
  "README.md": "# MyGoldEX — prototype Android 0.2.0\n\nInterface noire et or, panneau flottant déplaçable/réductible avec transparence,\nimport CSV et calculs locaux EMA 20/50, ATR de Wilder 14, plage 20 bougies,\nbalayage potentiel et FVG sur les dernières bougies. Android 8 minimum.\n\n## Limites\nPas d'IA connectée, de données temps réel, de capture d'écran ou d'ordres.\nLa surcouche ne lit pas MT5/TradingView et ne suit pas leur graphique.\nLe symbole et le timeframe sont déclarés par l'utilisateur.\nLes calculs ne constituent pas des signaux validés.\nCertains appareils/applications peuvent bloquer la surcouche.\nLes tests du moteur ne remplacent pas les essais sur téléphone.\n\n## Importer\n60 à 5 000 bougies clôturées et chronologiques, sans doublons.\nEn-tête exact : time,open,high,low,close\nDates Unix en secondes UTC ; décimales avec un point.\nexamples/SYNTHETIQUE_M5.csv contient uniquement des données synthétiques.\nChoisir le fichier puis ouvrir le panneau flottant.\n\n## Afficher sur MetaTrader 5\nInstaller MT5 officiel dans le même profil Android que MyGoldEX.\nAppuyer sur « Afficher sur MetaTrader 5 », accorder la permission d'affichage\npar-dessus les applications, puis revenir à MyGoldEX. Le panneau compact\napparaît avant l'ouverture de MT5. Le refus de permission n'ouvre aucun panneau.\nDéplacer le titre, appuyer sur « Ouvrir » pour l'analyse ou « Fermer » pour arrêter.\nLa position et la transparence sont mémorisées ; le panneau s'adapte à la rotation.\n\nGitHub Actions tente un test réel avec l'APK lié par le site officiel MetaQuotes\nsur un émulateur Android 14. Le test vérifie refus/retour d'autorisation, MT5 au\npremier plan, panneau visible, ouverture/réduction, déplacement, rotation et fermeture.\nLes captures, hiérarchies de fenêtres et rapports sont dans les artefacts de la CI.\nUne compilation seule ne prouve pas la réussite de ce test : consulter son résultat.\nLe test ne crée pas de compte MT5, n'accepte pas ses conditions et ne passe aucun ordre.\nIl ne garantit pas l'affichage sur chaque téléphone ni sur les écrans protégés de MT5.\n\n## Construire\nJDK 17, Android SDK 35, Build Tools 35.0.0, Gradle 8.11.1.\nGitHub Actions génère le Wrapper Gradle officiel, exécute les tests et assemble l'APK.\nDepuis les sources téléchargées, lancer :\n./gradlew :app:testDebugUnitTest :app:assembleDebug\n\n## Installer\nL'APK debug est destiné aux essais, pas à une distribution de production.\nSa clé debug peut changer entre deux exécutions GitHub, ce qui peut empêcher une\nmise à jour par-dessus une installation précédente. Prévoir une clé stable avant\ndistribution durable. Ne pas publier de clés privées dans le dépôt.\n\n## Confidentialité\nAucune permission Internet dans cette version. Aucun envoi de données.\nFichier choisi explicitement par l'utilisateur ; dernière analyse effaçable dans l'app.\n",
  "app/build.gradle.kts": "plugins {\n    id(\"com.android.application\")\n    id(\"org.jetbrains.kotlin.android\")\n}\nandroid {\n    namespace = \"com.mygoldex\"\n    compileSdk = 35\n    buildToolsVersion = \"35.0.0\"\n    defaultConfig {\n        applicationId = \"com.mygoldex\"\n        minSdk = 26\n        targetSdk = 35\n        versionCode = 2\n        versionName = \"0.2.0\"\n        testInstrumentationRunner = \"androidx.test.runner.AndroidJUnitRunner\"\n    }\n    compileOptions {\n        sourceCompatibility = JavaVersion.VERSION_17\n        targetCompatibility = JavaVersion.VERSION_17\n    }\n    kotlinOptions { jvmTarget = \"17\" }\n}\ndependencies {\n    testImplementation(\"junit:junit:4.13.2\")\n    androidTestImplementation(\"androidx.test:runner:1.6.2\")\n    androidTestImplementation(\"androidx.test.ext:junit:1.2.1\")\n    androidTestImplementation(\"androidx.test.uiautomator:uiautomator:2.3.0\")\n}\n",
  "app/src/androidTest/java/com/mygoldex/Mt5OverlayTest.kt": "package com.mygoldex\n\nimport android.content.Intent\nimport android.os.SystemClock\nimport androidx.test.ext.junit.runners.AndroidJUnit4\nimport androidx.test.platform.app.InstrumentationRegistry\nimport androidx.test.uiautomator.By\nimport androidx.test.uiautomator.UiDevice\nimport androidx.test.uiautomator.Until\nimport org.junit.Assert.*\nimport org.junit.Test\nimport org.junit.runner.RunWith\nimport java.io.File\nimport java.util.regex.Pattern\n\n@RunWith(AndroidJUnit4::class)\nclass Mt5OverlayTest {\n    private val context = InstrumentationRegistry.getInstrumentation().targetContext\n    private val device = UiDevice.getInstance(InstrumentationRegistry.getInstrumentation())\n    private val handle = By.text(\"MyGoldEX • Déplacer ici\")\n    private fun button(text: String) = By.text(Pattern.compile(Pattern.quote(text),\n        Pattern.CASE_INSENSITIVE or Pattern.UNICODE_CASE))\n    private fun eventually(message: String, condition: () -> Boolean) {\n        val deadline = SystemClock.elapsedRealtime() + 15000\n        while (SystemClock.elapsedRealtime() < deadline) {\n            if (condition()) return\n            SystemClock.sleep(200)\n        }\n        fail(message)\n    }\n    private fun evidence(name: String) {\n        val dir = File(context.getExternalFilesDir(null), \"evidence\").apply { mkdirs() }\n        device.takeScreenshot(File(dir, \"$name.png\"))\n        device.dumpWindowHierarchy(File(dir, \"$name.xml\"))\n        File(dir, \"$name-activities.txt\").writeText(\n            device.executeShellCommand(\"dumpsys activity activities\"))\n        File(dir, \"$name-windows.txt\").writeText(\n            device.executeShellCommand(\"dumpsys window windows\"))\n    }\n    private fun mt5Resumed(): Boolean = device.executeShellCommand(\"dumpsys activity activities\")\n        .lineSequence().any { it.contains(\"ResumedActivity\") && it.contains(MainActivity.MT5) }\n\n    @Test fun permissionReturnAndPanelAboveOfficialMt5() {\n        try {\n            assertNotNull(\"L'APK officiel MT5 doit être installé pour ce test\",\n                context.packageManager.getLaunchIntentForPackage(MainActivity.MT5))\n            device.setOrientationNatural()\n            // Autorisations accordées uniquement par le banc de test, jamais par l'application.\n            device.executeShellCommand(\"pm grant com.mygoldex android.permission.POST_NOTIFICATIONS\")\n            device.executeShellCommand(\"appops set com.mygoldex SYSTEM_ALERT_WINDOW deny\")\n            context.startActivity(Intent(context, MainActivity::class.java)\n                .addFlags(Intent.FLAG_ACTIVITY_NEW_TASK or Intent.FLAG_ACTIVITY_CLEAR_TASK))\n            assertTrue(device.wait(Until.hasObject(button(\"Afficher sur MetaTrader 5\")), 10000))\n            device.findObject(button(\"Afficher sur MetaTrader 5\")).click()\n            assertTrue(\"Les réglages d'autorisation doivent s'ouvrir\",\n                device.wait(Until.hasObject(By.pkg(\"com.android.settings\")), 10000))\n            device.pressBack()\n            assertTrue(device.wait(Until.hasObject(button(\"Afficher sur MetaTrader 5\")), 10000))\n            assertFalse(\"Pas de panneau sans autorisation\", device.hasObject(handle))\n            assertFalse(\"MT5 ne doit pas s'ouvrir si la permission est refusée\", mt5Resumed())\n\n            device.findObject(button(\"Afficher sur MetaTrader 5\")).click()\n            assertTrue(device.wait(Until.hasObject(By.pkg(\"com.android.settings\")), 10000))\n            device.executeShellCommand(\"appops set com.mygoldex SYSTEM_ALERT_WINDOW allow\")\n            device.pressBack()\n            eventually(\"MT5 doit être au premier plan après le retour des réglages\") { mt5Resumed() }\n            assertTrue(\"Le panneau doit apparaître avec MT5\", device.wait(Until.hasObject(handle), 10000))\n            SystemClock.sleep(3000)\n            assertTrue(\"Le panneau reste présent après le démarrage de MT5\", device.hasObject(handle))\n            evidence(\"01-mt5-panneau-compact\")\n            assertTrue(device.hasObject(button(\"Ouvrir\")))\n            device.findObject(button(\"Ouvrir\")).click()\n            assertTrue(device.wait(Until.hasObject(button(\"Réduire\")), 5000))\n            assertTrue(\"MT5 reste au premier plan pendant l'interaction\", mt5Resumed())\n            evidence(\"02-mt5-panneau-ouvert\")\n            device.findObject(button(\"Réduire\")).click()\n            assertTrue(device.wait(Until.hasObject(button(\"Ouvrir\")), 5000))\n            val before = device.findObject(handle).visibleBounds\n            device.drag(before.centerX(), before.centerY(), before.centerX(),\n                (before.centerY() + 150).coerceAtMost(device.displayHeight - 200), 30)\n            eventually(\"Le titre doit permettre de déplacer le panneau\") {\n                device.findObject(handle).visibleBounds.top != before.top\n            }\n            device.setOrientationLeft()\n            SystemClock.sleep(1500)\n            assertTrue(\"Le panneau reste accessible après rotation\", device.hasObject(handle))\n            evidence(\"03-mt5-rotation\")\n            device.findObject(button(\"Fermer\")).click()\n            assertTrue(\"Fermer retire le panneau\", device.wait(Until.gone(handle), 5000))\n        } finally {\n            evidence(\"04-etat-final\")\n            device.unfreezeRotation()\n        }\n    }\n}\n",
  "app/src/main/AndroidManifest.xml": "<?xml version=\"1.0\" encoding=\"utf-8\"?>\n<manifest xmlns:android=\"http://schemas.android.com/apk/res/android\">\n    <queries>\n        <package android:name=\"net.metaquotes.metatrader5\" />\n    </queries>\n    <uses-permission android:name=\"android.permission.SYSTEM_ALERT_WINDOW\" />\n    <uses-permission android:name=\"android.permission.FOREGROUND_SERVICE\" />\n    <uses-permission android:name=\"android.permission.FOREGROUND_SERVICE_SPECIAL_USE\" />\n    <uses-permission android:name=\"android.permission.POST_NOTIFICATIONS\" />\n    <application android:allowBackup=\"false\" android:label=\"MyGoldEX\"\n        android:supportsRtl=\"true\"\n        android:theme=\"@android:style/Theme.Material.NoActionBar\">\n        <activity android:name=\".MainActivity\" android:exported=\"true\">\n            <intent-filter>\n                <action android:name=\"android.intent.action.MAIN\" />\n                <category android:name=\"android.intent.category.LAUNCHER\" />\n            </intent-filter>\n        </activity>\n        <service android:name=\".OverlayService\" android:exported=\"false\"\n            android:foregroundServiceType=\"specialUse\">\n            <property android:name=\"android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE\"\n                android:value=\"User-started floating panel displaying local CSV analysis with explicit stop controls.\" />\n        </service>\n    </application>\n</manifest>\n",
  "app/src/main/java/com/mygoldex/MainActivity.kt": "package com.mygoldex\n\nimport android.Manifest\nimport android.app.Activity\nimport android.app.AlertDialog\nimport android.content.Intent\nimport android.content.pm.PackageManager\nimport android.graphics.Color\nimport android.net.Uri\nimport android.os.Build\nimport android.os.Bundle\nimport android.os.Handler\nimport android.os.Looper\nimport android.os.ResultReceiver\nimport android.provider.Settings\nimport android.widget.*\nimport java.util.concurrent.Executors\n\nclass MainActivity : Activity() {\n    private lateinit var report: TextView\n    private lateinit var symbol: EditText\n    private lateinit var timeframe: Spinner\n    private lateinit var importButton: Button\n    private lateinit var status: TextView\n    private var pendingOverlay = false\n    private var pendingMt5 = false\n    private var permissionInFlight = false\n    private var notificationAsked = false\n    private var readyToOpenMt5 = false\n    private var resumed = false\n    private var starting = false\n    private val handler = Handler(Looper.getMainLooper())\n    private val timeout = Runnable {\n        if (starting) {\n            starting = false\n            pendingMt5 = false\n            toast(\"Le panneau ne répond pas. Vérifiez l'autorisation de superposition.\")\n        }\n    }\n    private val worker = Executors.newSingleThreadExecutor()\n    private val prefs by lazy { getSharedPreferences(\"mygoldex\", MODE_PRIVATE) }\n\n    override fun onCreate(state: Bundle?) {\n        super.onCreate(state)\n        pendingOverlay = state?.getBoolean(\"pendingOverlay\") ?: false\n        pendingMt5 = state?.getBoolean(\"pendingMt5\") ?: false\n        notificationAsked = state?.getBoolean(\"notificationAsked\") ?: false\n        val root = LinearLayout(this).apply {\n            orientation = LinearLayout.VERTICAL\n            setBackgroundColor(Color.rgb(13, 16, 21))\n        }\n        root.setOnApplyWindowInsetsListener { view, insets ->\n            view.setPadding(\n                dp(16) + insets.systemWindowInsetLeft,\n                dp(12) + insets.systemWindowInsetTop,\n                dp(16) + insets.systemWindowInsetRight,\n                dp(12) + insets.systemWindowInsetBottom)\n            insets\n        }\n        val body = LinearLayout(this).apply { orientation = LinearLayout.VERTICAL }\n        val scroll = ScrollView(this)\n        scroll.addView(body)\n        root.addView(scroll)\n        setContentView(root)\n        root.requestApplyInsets()\n\n        fun label(value: String, size: Float) {\n            body.addView(TextView(this).apply {\n                text = value\n                textSize = size\n                setTextColor(Color.rgb(225, 185, 80))\n                setPadding(0, dp(12), 0, dp(8))\n            })\n        }\n        fun button(value: String, action: () -> Unit): Button {\n            return Button(this).also {\n                it.text = value\n                it.setOnClickListener { action() }\n                body.addView(it)\n            }\n        }\n        label(\"MyGoldEX\", 30f)\n        label(\"PANNEAU MT5 • VERSION 0.2\", 13f)\n        label(\"CSV uniquement. IA et trading réel non connectés.\", 14f)\n        status = TextView(this).apply {\n            textSize = 14f\n            setTextColor(Color.WHITE)\n            setPadding(0, dp(8), 0, dp(8))\n        }\n        body.addView(status)\n        button(\"Afficher sur MetaTrader 5\") { requestOverlay(true) }\n        button(\"Aide à la superposition\") {\n            AlertDialog.Builder(this).setTitle(\"MyGoldEX sur MetaTrader 5\")\n                .setMessage(\"1. Autorisez l'affichage par-dessus les applications.\\n\" +\n                    \"2. Appuyez sur Afficher sur MetaTrader 5.\\n\" +\n                    \"3. Déplacez le panneau par son titre ; Ouvrir affiche l'analyse.\\n\\n\" +\n                    \"Si Android l'arrête, vérifiez manuellement les réglages de batterie \" +\n                    \"et de lancement en arrière-plan de MyGoldEX.\\n\\n\" +\n                    \"Certains écrans protégés peuvent masquer les superpositions. \" +\n                    \"Le panneau n'accède pas aux prix ou aux ordres de MT5.\")\n                .setPositiveButton(\"Compris\", null).show()\n        }\n        symbol = EditText(this).apply {\n            hint = \"Symbole et fournisseur\"\n            setSingleLine()\n            setText(prefs.getString(\"symbol\", \"XAUUSD\"))\n            setTextColor(Color.WHITE)\n            setHintTextColor(Color.LTGRAY)\n        }\n        body.addView(symbol)\n        val frames = listOf(\"M1\", \"M5\", \"M15\", \"M30\", \"H1\", \"H4\", \"D1\")\n        timeframe = Spinner(this).apply {\n            adapter = ArrayAdapter(this@MainActivity,\n                android.R.layout.simple_spinner_dropdown_item, frames)\n            setSelection(frames.indexOf(prefs.getString(\"timeframe\", \"M5\"))\n                .coerceAtLeast(0))\n        }\n        body.addView(timeframe)\n        importButton = button(\"Importer un CSV\") {\n            AlertDialog.Builder(this)\n                .setTitle(\"Vérifier avant l'import\")\n                .setMessage(\"Le symbole et le timeframe sont déclarés par vous. \" +\n                    \"Le fichier doit contenir uniquement des bougies clôturées \" +\n                    \"et chronologiques.\")\n                .setNegativeButton(\"Annuler\", null)\n                .setPositiveButton(\"Choisir\") { _, _ ->\n                    startActivityForResult(\n                        Intent(Intent.ACTION_OPEN_DOCUMENT).apply {\n                            type = \"*/*\"\n                            addCategory(Intent.CATEGORY_OPENABLE)\n                        }, 100)\n                }.show()\n        }\n        button(\"Afficher le panneau flottant\") { requestOverlay(false) }\n        button(\"Fermer le panneau flottant\") {\n            stopService(Intent(this, OverlayService::class.java))\n        }\n        button(\"Effacer l'analyse\") {\n            prefs.edit().remove(\"report\").apply()\n            report.text = \"Aucune analyse.\"\n        }\n        label(\"DERNIÈRE ANALYSE\", 14f)\n        report = TextView(this).apply {\n            textSize = 15f\n            setTextColor(Color.WHITE)\n            setTextIsSelectable(true)\n            text = prefs.getString(\"report\", \"Aucune analyse.\")\n            setPadding(0, dp(8), 0, dp(20))\n        }\n        body.addView(report)\n    }\n\n    private fun requestOverlay(openMt5: Boolean) {\n        if (starting) return\n        if (openMt5 && packageManager.getLaunchIntentForPackage(MT5) == null) {\n            toast(\"MetaTrader 5 officiel n'est pas installé dans ce profil Android.\")\n            return\n        }\n        pendingOverlay = true\n        pendingMt5 = openMt5\n        continueOverlay()\n    }\n\n    private fun continueOverlay() {\n        if (!pendingOverlay || permissionInFlight || starting) return\n        if (!Settings.canDrawOverlays(this)) {\n            permissionInFlight = true\n            try {\n                startActivityForResult(Intent(Settings.ACTION_MANAGE_OVERLAY_PERMISSION,\n                    Uri.parse(\"package:\" + packageName)), 102)\n            } catch (_: Exception) {\n                permissionInFlight = false\n                pendingOverlay = false\n                toast(\"Autorisation de superposition indisponible.\")\n            }\n            return\n        }\n        if (Build.VERSION.SDK_INT >= 33 && !notificationAsked &&\n            checkSelfPermission(Manifest.permission.POST_NOTIFICATIONS) !=\n            PackageManager.PERMISSION_GRANTED) {\n            notificationAsked = true\n            permissionInFlight = true\n            requestPermissions(arrayOf(Manifest.permission.POST_NOTIFICATIONS), 101)\n            return\n        }\n        starting = true\n        pendingOverlay = false\n        val callback = object : ResultReceiver(handler) {\n            override fun onReceiveResult(code: Int, data: Bundle?) {\n                if (isDestroyed || isFinishing) return\n                handler.removeCallbacks(timeout)\n                starting = false\n                if (code != 1) {\n                    pendingMt5 = false\n                    toast(\"Le panneau n'a pas pu être affiché.\")\n                    return\n                }\n                readyToOpenMt5 = pendingMt5\n                pendingMt5 = false\n                openMt5IfReady()\n            }\n        }\n        try {\n            startForegroundService(Intent(this, OverlayService::class.java)\n                .putExtra(\"ready\", callback).putExtra(\"compact\", pendingMt5))\n            handler.postDelayed(timeout, 5000)\n        } catch (_: Exception) {\n            starting = false\n            pendingMt5 = false\n            toast(\"Impossible de démarrer le panneau.\")\n        }\n    }\n\n    private fun openMt5IfReady() {\n        if (!readyToOpenMt5 || !resumed) return\n        readyToOpenMt5 = false\n        val launch = packageManager.getLaunchIntentForPackage(MT5)\n        if (launch == null) { toast(\"MetaTrader 5 introuvable.\"); return }\n        try { startActivity(launch) }\n        catch (_: Exception) { toast(\"Ouvrez MetaTrader 5 manuellement ; le panneau est actif.\") }\n    }\n\n    override fun onResume() {\n        super.onResume()\n        resumed = true\n        status.text = \"MetaTrader 5 : \" +\n            (if (packageManager.getLaunchIntentForPackage(MT5) != null) \"installé\" else \"non détecté\") +\n            \"\\nSuperposition : \" + (if (Settings.canDrawOverlays(this)) \"autorisée\" else \"à autoriser\")\n        if (pendingOverlay && !permissionInFlight) continueOverlay()\n        openMt5IfReady()\n    }\n    override fun onPause() { resumed = false; super.onPause() }\n    override fun onSaveInstanceState(out: Bundle) {\n        out.putBoolean(\"pendingOverlay\", pendingOverlay || starting)\n        out.putBoolean(\"pendingMt5\", pendingMt5 || readyToOpenMt5)\n        out.putBoolean(\"notificationAsked\", notificationAsked)\n        super.onSaveInstanceState(out)\n    }\n    override fun onRequestPermissionsResult(code: Int, permissions: Array<out String>, results: IntArray) {\n        super.onRequestPermissionsResult(code, permissions, results)\n        if (code == 101) { permissionInFlight = false; continueOverlay() }\n    }\n\n    @Deprecated(\"API native conservée pour cette version\")\n    override fun onActivityResult(requestCode: Int, resultCode: Int, data: Intent?) {\n        super.onActivityResult(requestCode, resultCode, data)\n        if (requestCode == 102) {\n            permissionInFlight = false\n            if (Settings.canDrawOverlays(this)) continueOverlay()\n            else {\n                pendingOverlay = false\n                pendingMt5 = false\n                toast(\"Superposition non autorisée. Aucun panneau ouvert.\")\n            }\n            return\n        }\n        if (requestCode != 100 || resultCode != RESULT_OK) return\n        val uri = data?.data ?: return\n        val asset = symbol.text.toString().trim().take(80)\n        val frame = timeframe.selectedItem.toString()\n        if (asset.isBlank()) {\n            toast(\"Renseignez le symbole.\")\n            return\n        }\n        importButton.isEnabled = false\n        importButton.text = \"Analyse en cours…\"\n        worker.execute {\n            val result = runCatching {\n                val input = contentResolver.openInputStream(uri)\n                    ?: error(\"Lecture impossible.\")\n                val csv = input.bufferedReader().use { reader ->\n                    val buffer = CharArray(4096)\n                    val text = StringBuilder()\n                    while (true) {\n                        if (Thread.currentThread().isInterrupted) error(\"Import annulé.\")\n                        val count = reader.read(buffer)\n                        if (count < 0) break\n                        require(text.length + count <= 2_000_000) {\n                            \"Fichier trop volumineux.\"\n                        }\n                        text.append(buffer, 0, count)\n                    }\n                    text.toString()\n                }\n                asset + \" • \" + frame + \"\\nIdentification déclarée\\n\\n\" +\n                    MarketEngine.analyze(MarketEngine.parseCsv(csv))\n            }\n            runOnUiThread {\n                if (isDestroyed || isFinishing) return@runOnUiThread\n                importButton.isEnabled = true\n                importButton.text = \"Importer un CSV\"\n                result.onSuccess {\n                    report.text = it\n                    prefs.edit().putString(\"report\", it)\n                        .putString(\"symbol\", asset)\n                        .putString(\"timeframe\", frame).apply()\n                }.onFailure { toast(it.message ?: \"Erreur d'analyse.\") }\n            }\n        }\n    }\n    private fun dp(value: Int) = (value * resources.displayMetrics.density).toInt()\n    private fun toast(value: String) {\n        Toast.makeText(this, value, Toast.LENGTH_LONG).show()\n    }\n    override fun onDestroy() {\n        worker.shutdownNow()\n        handler.removeCallbacksAndMessages(null)\n        super.onDestroy()\n    }\n    companion object { const val MT5 = \"net.metaquotes.metatrader5\" }\n}\n",
  "app/src/main/java/com/mygoldex/MarketEngine.kt": "package com.mygoldex\n\nimport java.time.Instant\nimport java.util.Locale\nimport kotlin.math.abs\nimport kotlin.math.max\nimport kotlin.math.min\n\ndata class Candle(val time: Long, val open: Double, val high: Double,\n                  val low: Double, val close: Double)\n\nobject MarketEngine {\n    private fun fmt(value: Double) = String.format(Locale.US, \"%.5f\", value)\n\n    fun parseCsv(csv: String): List<Candle> {\n        val lines = csv.removePrefix(\"\\uFEFF\").lineSequence()\n            .filter { it.isNotBlank() }.toList()\n        require(lines.isNotEmpty()) { \"Fichier vide.\" }\n        require(lines.first().trim().lowercase(Locale.ROOT) ==\n            \"time,open,high,low,close\") {\n            \"En-tête requis : time,open,high,low,close\"\n        }\n        require(lines.size in 61..5001) { \"Il faut entre 60 et 5 000 bougies.\" }\n        val now = Instant.now().epochSecond\n        val candles = lines.drop(1).mapIndexed { index, line ->\n            val row = line.split(\",\").map { it.trim() }\n            val position = \" Ligne \" + (index + 2) + \".\"\n            require(row.size == 5) { \"Cinq colonnes requises.\" + position }\n            val time = row[0].toLongOrNull()\n                ?: error(\"Horodatage invalide.\" + position)\n            require(time in 1..now) { \"Horodatage hors limites.\" + position }\n            val prices = row.drop(1).map {\n                it.toDoubleOrNull()?.takeIf { n -> n.isFinite() && n > 0 }\n                    ?: error(\"Prix invalide.\" + position)\n            }\n            Candle(time, prices[0], prices[1], prices[2], prices[3]).also {\n                require(it.high >= max(it.open, it.close) &&\n                    it.low <= min(it.open, it.close) && it.high >= it.low) {\n                    \"OHLC incohérent.\" + position\n                }\n            }\n        }\n        require(candles.zipWithNext().all { (a,b) -> a.time < b.time }) {\n            \"Dates non chronologiques ou doublons.\"\n        }\n        return candles\n    }\n\n    private fun ema(values: List<Double>, period: Int): Double {\n        val alpha = 2.0 / (period + 1)\n        var result = values.take(period).average()\n        for (value in values.drop(period)) {\n            result = alpha * value + (1 - alpha) * result\n        }\n        return result\n    }\n\n    fun analyze(candles: List<Candle>): String {\n        require(candles.size >= 60)\n        val last = candles.last()\n        val closes = candles.map { it.close }\n        val fast = ema(closes, 20)\n        val slow = ema(closes, 50)\n        val ranges = candles.zipWithNext().map { (previous, current) ->\n            max(current.high - current.low,\n                max(abs(current.high - previous.close),\n                    abs(current.low - previous.close)))\n        }\n        var atr = ranges.take(14).average()\n        for (range in ranges.drop(14)) atr = (atr * 13 + range) / 14\n        val previous = candles.dropLast(1).takeLast(20)\n        val support = previous.minOf { it.low }\n        val resistance = previous.maxOf { it.high }\n        val bias = when {\n            last.close > fast && fast > slow -> \"HAUSSIER\"\n            last.close < fast && fast < slow -> \"BAISSIER\"\n            else -> \"NEUTRE / MIXTE\"\n        }\n        val structure = when {\n            last.close > resistance -> \"Cassure haussière de la plage 20 bougies\"\n            last.close < support -> \"Cassure baissière de la plage 20 bougies\"\n            else -> \"Clôture dans la plage des 20 bougies précédentes\"\n        }\n        val sweep = when {\n            last.low < support && last.close > support ->\n                \"Plus bas dépassé puis réintégré\"\n            last.high > resistance && last.close < resistance ->\n                \"Plus haut dépassé puis réintégré\"\n            else -> \"Aucun sur la dernière bougie\"\n        }\n        val first = candles[candles.lastIndex - 2]\n        val fvg = when {\n            last.low > first.high ->\n                \"Haussier : \" + fmt(first.high) + \" – \" + fmt(last.low)\n            last.high < first.low ->\n                \"Baissier : \" + fmt(last.high) + \" – \" + fmt(first.low)\n            else -> \"Aucun sur les trois dernières bougies\"\n        }\n        return listOf(\n            \"IMPORT CSV — PAS DE DIRECT\",\n            \"Dernière bougie UTC : \" + Instant.ofEpochSecond(last.time),\n            \"Bougies : \" + candles.size,\n            \"Clôture : \" + fmt(last.close),\n            \"\",\n            \"BIAIS : \" + bias,\n            \"EMA 20 : \" + fmt(fast),\n            \"EMA 50 : \" + fmt(slow),\n            \"ATR 14 : \" + fmt(atr),\n            \"\",\n            \"SUPPORT : \" + fmt(support),\n            \"RÉSISTANCE : \" + fmt(resistance),\n            \"\",\n            \"STRUCTURE\", structure,\n            \"\", \"BALAYAGE POTENTIEL\", sweep,\n            \"\", \"DERNIER FVG\", fvg,\n            \"\", \"Calculs locaux, sans IA.\",\n            \"Aucun signal de trading validé.\",\n            \"Aucun ordre transmis.\"\n        ).joinToString(\"\\n\")\n    }\n}\n",
  "app/src/main/java/com/mygoldex/OverlayService.kt": "package com.mygoldex\n\nimport android.app.*\nimport android.content.Intent\nimport android.content.SharedPreferences\nimport android.content.pm.ServiceInfo\nimport android.content.res.Configuration\nimport android.graphics.Color\nimport android.graphics.PixelFormat\nimport android.graphics.drawable.GradientDrawable\nimport android.graphics.drawable.Icon\nimport android.os.Build\nimport android.os.IBinder\nimport android.os.ResultReceiver\nimport android.provider.Settings\nimport android.view.*\nimport android.widget.*\n\nclass OverlayService : Service(), SharedPreferences.OnSharedPreferenceChangeListener {\n    private lateinit var manager: WindowManager\n    private lateinit var panel: LinearLayout\n    private lateinit var params: WindowManager.LayoutParams\n    private lateinit var report: TextView\n    private lateinit var details: LinearLayout\n    private lateinit var toggle: Button\n    private lateinit var scroll: ScrollView\n    private var compact = false\n    private var attached = false\n    private val prefs by lazy { getSharedPreferences(\"mygoldex\", MODE_PRIVATE) }\n\n    override fun onCreate() {\n        super.onCreate()\n        val notifications = getSystemService(NotificationManager::class.java)\n        notifications.createNotificationChannel(NotificationChannel(\n            \"overlay\", \"Panneau MyGoldEX\", NotificationManager.IMPORTANCE_LOW))\n        val stop = PendingIntent.getService(this, 1,\n            Intent(this, OverlayService::class.java).setAction(\"STOP\"),\n            PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE)\n        val open = PendingIntent.getActivity(this, 2,\n            Intent(this, MainActivity::class.java),\n            PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE)\n        val notification = Notification.Builder(this, \"overlay\")\n            .setSmallIcon(android.R.drawable.ic_dialog_info)\n            .setContentTitle(\"MyGoldEX\")\n            .setContentText(\"Analyse locale — aucune lecture de l'écran\")\n            .setContentIntent(open).setOngoing(true)\n            .addAction(Notification.Action.Builder(null as Icon?, \"Fermer\", stop).build())\n            .build()\n        if (Build.VERSION.SDK_INT >= 34) {\n            startForeground(1, notification,\n                ServiceInfo.FOREGROUND_SERVICE_TYPE_SPECIAL_USE)\n        } else startForeground(1, notification)\n        if (!Settings.canDrawOverlays(this)) {\n            stopSelf()\n            return\n        }\n        manager = getSystemService(WindowManager::class.java)\n        createPanel()\n    }\n\n    private fun createPanel() {\n        val screen = resources.displayMetrics\n        params = WindowManager.LayoutParams(\n            minOf(dp(310), screen.widthPixels - dp(16)),\n            WindowManager.LayoutParams.WRAP_CONTENT,\n            WindowManager.LayoutParams.TYPE_APPLICATION_OVERLAY,\n            WindowManager.LayoutParams.FLAG_NOT_FOCUSABLE or\n                WindowManager.LayoutParams.FLAG_NOT_TOUCH_MODAL,\n            PixelFormat.TRANSLUCENT).apply {\n            gravity = Gravity.TOP or Gravity.START\n            title = \"MyGoldEX overlay\"\n            x = prefs.getInt(\"overlayX\", dp(8))\n            y = prefs.getInt(\"overlayY\", dp(60))\n            alpha = prefs.getFloat(\"overlayAlpha\", 1f).coerceIn(0.7f, 1f)\n        }\n        panel = LinearLayout(this).apply {\n            orientation = LinearLayout.VERTICAL\n            setPadding(dp(12), dp(10), dp(12), dp(10))\n            background = GradientDrawable().apply {\n                setColor(Color.rgb(18, 22, 29))\n                cornerRadius = dp(16).toFloat()\n                setStroke(dp(1), Color.rgb(225, 185, 80))\n            }\n        }\n        val handle = TextView(this).apply {\n            text = \"MyGoldEX • Déplacer ici\"\n            textSize = 18f\n            setTextColor(Color.rgb(225, 185, 80))\n            setPadding(0, dp(10), 0, dp(16))\n        }\n        panel.addView(handle)\n        var downX = 0f\n        var downY = 0f\n        var initialX = 0\n        var initialY = 0\n        handle.setOnTouchListener { view, event ->\n            when (event.actionMasked) {\n                MotionEvent.ACTION_DOWN -> {\n                    downX = event.rawX\n                    downY = event.rawY\n                    initialX = params.x\n                    initialY = params.y\n                    true\n                }\n                MotionEvent.ACTION_MOVE -> {\n                    params.x = (initialX + event.rawX - downX).toInt()\n                        .coerceIn(0, (resources.displayMetrics.widthPixels -\n                            panel.width).coerceAtLeast(0))\n                    params.y = (initialY + event.rawY - downY).toInt()\n                        .coerceIn(0, (resources.displayMetrics.heightPixels -\n                            panel.height - dp(24)).coerceAtLeast(0))\n                    updatePanel()\n                    true\n                }\n                MotionEvent.ACTION_UP -> {\n                    prefs.edit().putInt(\"overlayX\", params.x)\n                        .putInt(\"overlayY\", params.y).apply()\n                    view.performClick()\n                    true\n                }\n                MotionEvent.ACTION_CANCEL -> true\n                else -> false\n            }\n        }\n        details = LinearLayout(this).apply { orientation = LinearLayout.VERTICAL }\n        report = TextView(this).apply { textSize = 14f; setTextColor(Color.WHITE) }\n        scroll = ScrollView(this).apply { addView(report) }\n        details.addView(scroll, LinearLayout.LayoutParams(\n            LinearLayout.LayoutParams.MATCH_PARENT,\n            minOf(dp(280), screen.heightPixels / 3)))\n        details.addView(TextView(this).apply {\n            text = \"Transparence\"; setTextColor(Color.LTGRAY)\n        })\n        details.addView(SeekBar(this).apply {\n            max = 30\n            progress = ((1f - params.alpha) * 100).toInt()\n            setOnSeekBarChangeListener(object : SeekBar.OnSeekBarChangeListener {\n                override fun onProgressChanged(\n                    bar: SeekBar?, progress: Int, fromUser: Boolean) {\n                    params.alpha = 1f - progress / 100f\n                    prefs.edit().putFloat(\"overlayAlpha\", params.alpha).apply()\n                    updatePanel()\n                }\n                override fun onStartTrackingTouch(bar: SeekBar?) {}\n                override fun onStopTrackingTouch(bar: SeekBar?) {}\n            })\n        })\n        panel.addView(details)\n        val controls = LinearLayout(this)\n        toggle = Button(this).apply {\n            text = \"Réduire\"\n            setOnClickListener {\n                setCompact(!compact)\n            }\n        }\n        controls.addView(toggle, LinearLayout.LayoutParams(0, dp(48), 1f))\n        controls.addView(Button(this).apply {\n            text = \"Fermer\"; setOnClickListener { stopSelf() }\n        }, LinearLayout.LayoutParams(0, dp(48), 1f))\n        panel.addView(controls)\n        refreshReport()\n        try {\n            manager.addView(panel, params)\n            attached = true\n            panel.addOnLayoutChangeListener { _, _, _, _, _, _, _, _, _ -> clampPosition() }\n            panel.post { clampPosition() }\n            prefs.registerOnSharedPreferenceChangeListener(this)\n        } catch (_: Exception) {\n            Toast.makeText(this, \"Affichage du panneau impossible.\", Toast.LENGTH_LONG).show()\n            stopSelf()\n        }\n    }\n    private fun setCompact(value: Boolean) {\n        compact = value\n        details.visibility = if (value) View.GONE else View.VISIBLE\n        toggle.text = if (value) \"Ouvrir\" else \"Réduire\"\n        params.width = minOf(dp(if (value) 260 else 310),\n            resources.displayMetrics.widthPixels - dp(16))\n        updatePanel()\n        panel.post { clampPosition() }\n    }\n    private fun clampPosition() {\n        if (!attached || panel.width == 0 || panel.height == 0) return\n        val screen = resources.displayMetrics\n        val newX = params.x.coerceIn(0, (screen.widthPixels - panel.width).coerceAtLeast(0))\n        val newY = params.y.coerceIn(0, (screen.heightPixels - panel.height - dp(24)).coerceAtLeast(0))\n        if (params.x != newX || params.y != newY) {\n            params.x = newX\n            params.y = newY\n            updatePanel()\n        }\n    }\n    override fun onConfigurationChanged(config: Configuration) {\n        super.onConfigurationChanged(config)\n        if (!attached) return\n        scroll.layoutParams = scroll.layoutParams.apply {\n            height = minOf(dp(280), resources.displayMetrics.heightPixels / 3)\n        }\n        setCompact(compact)\n    }\n    private fun updatePanel() {\n        if (!attached) return\n        try { manager.updateViewLayout(panel, params) }\n        catch (_: Exception) { stopSelf() }\n    }\n    private fun refreshReport() {\n        report.text = prefs.getString(\"report\",\n            \"Aucune analyse.\\nImportez un CSV dans MyGoldEX.\")\n    }\n    override fun onSharedPreferenceChanged(sharedPreferences: SharedPreferences?, key: String?) {\n        if (key == \"report\") refreshReport()\n    }\n    override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {\n        if (intent?.action == \"STOP\") { stopSelf(); return START_NOT_STICKY }\n        @Suppress(\"DEPRECATION\")\n        val callback = intent?.getParcelableExtra<ResultReceiver>(\"ready\")\n        if (!attached || !Settings.canDrawOverlays(this)) {\n            callback?.send(0, null)\n            stopSelf()\n        } else {\n            setCompact(intent?.getBooleanExtra(\"compact\", false) ?: false)\n            panel.post { callback?.send(if (attached && panel.isShown) 1 else 0, null) }\n        }\n        return START_NOT_STICKY\n    }\n    override fun onDestroy() {\n        prefs.unregisterOnSharedPreferenceChangeListener(this)\n        if (attached) { runCatching { manager.removeView(panel) }; attached = false }\n        stopForeground(STOP_FOREGROUND_REMOVE)\n        super.onDestroy()\n    }\n    override fun onBind(intent: Intent?): IBinder? = null\n    private fun dp(value: Int) = (value * resources.displayMetrics.density).toInt()\n}\n",
  "app/src/test/java/com/mygoldex/MarketEngineTest.kt": "package com.mygoldex\n\nimport org.junit.Assert.*\nimport org.junit.Test\n\nclass MarketEngineTest {\n    private fun csv(count: Int = 60, duplicate: Boolean = false,\n                    prices: String = \"100,101,99,100\"): String {\n        val rows = (0 until count).map {\n            val time = 1704067200L + if (duplicate) 0L else it * 300L\n            time.toString() + \",\" + prices\n        }\n        return \"time,open,high,low,close\\n\" + rows.joinToString(\"\\n\")\n    }\n\n    @Test fun validImportHasSixtyCandles() {\n        assertEquals(60, MarketEngine.parseCsv(csv()).size)\n    }\n    @Test fun flatMarketHasNeutralBiasAndAtrTwo() {\n        val result = MarketEngine.analyze(MarketEngine.parseCsv(csv()))\n        assertTrue(result.contains(\"NEUTRE / MIXTE\"))\n        assertTrue(result.contains(\"ATR 14 : 2.00000\"))\n        assertTrue(result.contains(\"PAS DE DIRECT\"))\n    }\n    @Test(expected = IllegalArgumentException::class)\n    fun rejectsTooFewCandles() { MarketEngine.parseCsv(csv(count = 59)) }\n\n    @Test(expected = IllegalArgumentException::class)\n    fun rejectsDuplicates() { MarketEngine.parseCsv(csv(duplicate = true)) }\n\n    @Test(expected = IllegalArgumentException::class)\n    fun rejectsInvalidOhlc() {\n        MarketEngine.parseCsv(csv(prices = \"100,98,99,100\"))\n    }\n    @Test(expected = IllegalStateException::class)\n    fun rejectsNonFinitePrices() {\n        MarketEngine.parseCsv(csv(prices = \"NaN,101,99,100\"))\n    }\n}\n",
  "build.gradle.kts": "plugins {\n    id(\"com.android.application\") version \"8.9.2\" apply false\n    id(\"org.jetbrains.kotlin.android\") version \"2.1.20\" apply false\n}\n",
  "ci/download_mt5.py": "\"\"\"Download only the MT5 APK linked from the official MetaQuotes website.\"\"\"\nimport hashlib\nimport json\nfrom html.parser import HTMLParser\nfrom pathlib import Path\nfrom urllib.parse import urljoin, urlparse\nfrom urllib.request import Request, urlopen\n\nPAGE = \"https://www.metatrader5.com/en/mobile-trading/android\"\n\n\nclass Links(HTMLParser):\n    def __init__(self):\n        super().__init__()\n        self.links = []\n\n    def handle_starttag(self, tag, attrs):\n        if tag == \"a\":\n            self.links.extend(v for k, v in attrs if k == \"href\" and v)\n\n\ndef get(url):\n    return urlopen(Request(url, headers={\"User-Agent\": \"Mozilla/5.0\"}), timeout=90)\n\n\nparser = Links()\nwith get(PAGE) as response:\n    parser.feed(response.read().decode(\"utf-8\"))\ncandidates = [urljoin(PAGE, link) for link in parser.links]\ncandidates = [url for url in candidates if urlparse(url).scheme == \"https\"\n              and urlparse(url).hostname in {\"download.terminal.free\", \"download.mql5.com\"}\n              and urlparse(url).path.lower().endswith(\"/metatrader5.apk\")]\nif not candidates:\n    raise RuntimeError(\"Aucun lien APK officiel MT5 reconnu sur la page MetaQuotes\")\nurl = candidates[0]\nwith get(url) as response:\n    data = response.read(300 * 1024 * 1024 + 1)\nif not data.startswith(b\"PK\") or len(data) > 300 * 1024 * 1024:\n    raise RuntimeError(\"Téléchargement APK invalide\")\nPath(\"mt5.apk\").write_bytes(data)\nPath(\"evidence\").mkdir(exist_ok=True)\nmetadata = {\"official_page\": PAGE, \"apk_url\": url,\n            \"sha256\": hashlib.sha256(data).hexdigest(), \"size\": len(data)}\nPath(\"evidence/mt5-provenance.json\").write_text(json.dumps(metadata, indent=2))\nprint(json.dumps(metadata, indent=2))\n",
  "ci/test_mt5.sh": "#!/usr/bin/env bash\nset -euo pipefail\ncollect_evidence() {\n  adb logcat -d > evidence/logcat.txt || true\n  adb shell dumpsys activity activities > evidence/activities-final.txt || true\n  adb pull /sdcard/Android/data/com.mygoldex/files/evidence evidence/screenshots || true\n}\ntrap collect_evidence EXIT\nadb install -r mt5.apk\n(cd generated/MyGoldEX && bash ./gradlew :app:connectedDebugAndroidTest --no-daemon --stacktrace)\n",
  "examples/SYNTHETIQUE_M5.csv": "time,open,high,low,close\n1704067200,2000.00,2001.00,1999.00,2000.10\n1704067500,2000.20,2001.20,1999.20,2000.30\n1704067800,2000.40,2001.40,1999.40,2000.50\n1704068100,2000.60,2001.60,1999.60,2000.70\n1704068400,2000.80,2001.80,1999.80,2000.90\n1704068700,2001.00,2002.00,2000.00,2001.10\n1704069000,2001.20,2002.20,2000.20,2001.30\n1704069300,2001.40,2002.40,2000.40,2001.50\n1704069600,2001.60,2002.60,2000.60,2001.70\n1704069900,2001.80,2002.80,2000.80,2001.90\n1704070200,2002.00,2003.00,2001.00,2002.10\n1704070500,2002.20,2003.20,2001.20,2002.30\n1704070800,2002.40,2003.40,2001.40,2002.50\n1704071100,2002.60,2003.60,2001.60,2002.70\n1704071400,2002.80,2003.80,2001.80,2002.90\n1704071700,2003.00,2004.00,2002.00,2003.10\n1704072000,2003.20,2004.20,2002.20,2003.30\n1704072300,2003.40,2004.40,2002.40,2003.50\n1704072600,2003.60,2004.60,2002.60,2003.70\n1704072900,2003.80,2004.80,2002.80,2003.90\n1704073200,2004.00,2005.00,2003.00,2004.10\n1704073500,2004.20,2005.20,2003.20,2004.30\n1704073800,2004.40,2005.40,2003.40,2004.50\n1704074100,2004.60,2005.60,2003.60,2004.70\n1704074400,2004.80,2005.80,2003.80,2004.90\n1704074700,2005.00,2006.00,2004.00,2005.10\n1704075000,2005.20,2006.20,2004.20,2005.30\n1704075300,2005.40,2006.40,2004.40,2005.50\n1704075600,2005.60,2006.60,2004.60,2005.70\n1704075900,2005.80,2006.80,2004.80,2005.90\n1704076200,2006.00,2007.00,2005.00,2006.10\n1704076500,2006.20,2007.20,2005.20,2006.30\n1704076800,2006.40,2007.40,2005.40,2006.50\n1704077100,2006.60,2007.60,2005.60,2006.70\n1704077400,2006.80,2007.80,2005.80,2006.90\n1704077700,2007.00,2008.00,2006.00,2007.10\n1704078000,2007.20,2008.20,2006.20,2007.30\n1704078300,2007.40,2008.40,2006.40,2007.50\n1704078600,2007.60,2008.60,2006.60,2007.70\n1704078900,2007.80,2008.80,2006.80,2007.90\n1704079200,2008.00,2009.00,2007.00,2008.10\n1704079500,2008.20,2009.20,2007.20,2008.30\n1704079800,2008.40,2009.40,2007.40,2008.50\n1704080100,2008.60,2009.60,2007.60,2008.70\n1704080400,2008.80,2009.80,2007.80,2008.90\n1704080700,2009.00,2010.00,2008.00,2009.10\n1704081000,2009.20,2010.20,2008.20,2009.30\n1704081300,2009.40,2010.40,2008.40,2009.50\n1704081600,2009.60,2010.60,2008.60,2009.70\n1704081900,2009.80,2010.80,2008.80,2009.90\n1704082200,2010.00,2011.00,2009.00,2010.10\n1704082500,2010.20,2011.20,2009.20,2010.30\n1704082800,2010.40,2011.40,2009.40,2010.50\n1704083100,2010.60,2011.60,2009.60,2010.70\n1704083400,2010.80,2011.80,2009.80,2010.90\n1704083700,2011.00,2012.00,2010.00,2011.10\n1704084000,2011.20,2012.20,2010.20,2011.30\n1704084300,2011.40,2012.40,2010.40,2011.50\n1704084600,2011.60,2012.60,2010.60,2011.70\n1704084900,2011.80,2012.80,2010.80,2011.90\n1704085200,2012.00,2013.00,2011.00,2012.10\n1704085500,2012.20,2013.20,2011.20,2012.30\n1704085800,2012.40,2013.40,2011.40,2012.50\n1704086100,2012.60,2013.60,2011.60,2012.70\n1704086400,2012.80,2013.80,2011.80,2012.90\n1704086700,2013.00,2014.00,2012.00,2013.10\n1704087000,2013.20,2014.20,2012.20,2013.30\n1704087300,2013.40,2014.40,2012.40,2013.50\n1704087600,2013.60,2014.60,2012.60,2013.70\n1704087900,2013.80,2014.80,2012.80,2013.90\n1704088200,2014.00,2015.00,2013.00,2014.10\n1704088500,2014.20,2015.20,2013.20,2014.30\n1704088800,2014.40,2015.40,2013.40,2014.50\n1704089100,2014.60,2015.60,2013.60,2014.70\n1704089400,2014.80,2015.80,2013.80,2014.90\n1704089700,2015.00,2016.00,2014.00,2015.10\n1704090000,2015.20,2016.20,2014.20,2015.30\n1704090300,2015.40,2016.40,2014.40,2015.50\n1704090600,2015.60,2016.60,2014.60,2015.70\n1704090900,2015.80,2016.80,2014.80,2015.90\n1704091200,2016.00,2017.00,2015.00,2016.10\n1704091500,2016.20,2017.20,2015.20,2016.30\n1704091800,2016.40,2017.40,2015.40,2016.50\n1704092100,2016.60,2017.60,2015.60,2016.70\n1704092400,2016.80,2017.80,2015.80,2016.90\n1704092700,2017.00,2018.00,2016.00,2017.10\n1704093000,2017.20,2018.20,2016.20,2017.30\n1704093300,2017.40,2018.40,2016.40,2017.50\n1704093600,2017.60,2018.60,2016.60,2017.70\n1704093900,2017.80,2018.80,2016.80,2017.90\n1704094200,2018.00,2019.00,2017.00,2018.10\n1704094500,2018.20,2019.20,2017.20,2018.30\n1704094800,2018.40,2019.40,2017.40,2018.50\n1704095100,2018.60,2019.60,2017.60,2018.70\n1704095400,2018.80,2019.80,2017.80,2018.90\n1704095700,2019.00,2020.00,2018.00,2019.10\n1704096000,2019.20,2020.20,2018.20,2019.30\n1704096300,2019.40,2020.40,2018.40,2019.50\n1704096600,2019.60,2020.60,2018.60,2019.70\n1704096900,2019.80,2020.80,2018.80,2019.90\n",
  "gradle.properties": "org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8\nkotlin.code.style=official\nandroid.useAndroidX=true\n",
  "settings.gradle.kts": "pluginManagement {\n    repositories { google(); mavenCentral(); gradlePluginPortal() }\n}\ndependencyResolutionManagement {\n    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)\n    repositories { google(); mavenCentral() }\n}\nrootProject.name = \"MyGoldEX\"\ninclude(\":app\")\n"
}''')

def main():
    if ROOT.exists():
        raise SystemExit(
            f"Le dossier existe déjà : {ROOT}\n"
            "Conservez vos modifications et utilisez un dossier de travail neuf."
        )
    ROOT.mkdir(parents=True, exist_ok=False)
    for relative_path, content in FILES.items():
        destination = ROOT / relative_path
        destination.parent.mkdir(parents=True, exist_ok=True)
        with destination.open("x", encoding="utf-8", newline="\n") as stream:
            stream.write(content)
    print(f"Projet MyGoldEX créé : {ROOT} ({len(FILES)} fichiers)")

if __name__ == "__main__":
    main()
